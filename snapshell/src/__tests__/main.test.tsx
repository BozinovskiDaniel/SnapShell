import SectionOne from '@/app/home-page/SectionOne';
import Page from '@/app/page';
import { render, screen } from '@testing-library/react';

describe('group', () => {
  it('should', () => {
    expect(1).toBeTruthy();
  });
});
